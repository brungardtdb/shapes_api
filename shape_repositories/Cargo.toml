[package]
name = "shape_repositories"
version = "0.1.0"
edition = "2024"

[dependencies]
futures = "0.3"
shapes = { path = "../shapes" }
# tokio + native-tls
sqlx = { version = "0.8", features = [ "runtime-tokio", "tls-native-tls", "postgres" ] }